**作业批复：（批改环境：英伟达 DGX Spark）**

**一、问题：**

1.文档结构目录中没有 requirements.txt，且依赖库版本不对。应为pip install --upgrade pip setuptools wheel，pip install Flask==2.3.3 Flask-CORS==4.0.0 "opencv-python>=4.8" "Pillow>=10.0" "numpy>=1.26.0"。（独立venv环境下）

2.前端页面：
（1）合格与不合格照片给出理由

（2）语义搜索正确率低。

**二、优化建议：**

1\.补充完整依赖列表。

2\.优化模型准确度，或者选用更精准的开源模型。

3.增强功能逻辑。

4.其他增加demo能力的优化（如丰富使用场景功能，美观的UI等）



**复试相关：**

1.时间在元旦后，会提前2天通知。

2.复试要求：

（1）携带优化后的demo到公司进行部署。部署环境：英伟达 DGX Spark（可结合硬件条件，选择最合适的模型；以及更新部署文件）。

（2）部署好后，进行demo演示和讲解。可使用PPT。

